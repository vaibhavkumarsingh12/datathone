# SPEAR — offline analysis pipeline + Streamlit research console.
# Pinned to the exact versions every number in reports/*.json was produced with.
# (The deployed Catalyst read-layer has its own, much smaller, requirements.txt
#  at deploy/spear-catalyst/requirements.txt — Flask + waitress only.)

# --- data ---
pandas==2.2.3
numpy==1.26.4
scipy==1.15.2

# --- modelling ---
scikit-learn==1.9.0          # DBSCAN hotspots, GradientBoosting risk, IsolationForest
statsmodels==0.14.6          # OLS socio-economic regression

# --- entity resolution + network ---
RapidFuzz==3.14.5            # fuzzy name blocking
networkx==3.4.2              # co-offence graph, Louvain, centrality
pyvis==0.3.2                 # interactive graph rendering

# --- dashboard ---
streamlit==1.58.0
plotly==6.0.1
